{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Tunnel": {
    "//": "DEV-ONLY lease drive endpoints (Phase-1 test harness, no auth/billing). Never enable in prod.",
    "EnableDevEndpoints": true
  }
}
